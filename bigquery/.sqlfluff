[sqlfluff]
dialect = bigquery
# Exclude structure.column_order, capitalisation.identifiers
exclude_rules = L034, L014
max_line_length = 120
large_file_skip_byte_limit = 200000

[sqlfluff:rules]
# allow_scalar = True
# single_table_references = consistent
# unquoted_identifiers_policy = all

[sqlfluff:indentation]
tab_space_size = 2
indent_unit = space

# Force the reserved words to be capitalized (e.g., SELECT, FROM, etc.)。
[sqlfluff:rules:capitalisation.keywords]
capitalisation_policy = upper
[sqlfluff:rules:capitalisation.literals]
capitalisation_policy = upper

# Force the functions to be capitalized (e.g., SUM, COUNT, etc.)。
[sqlfluff:rules:capitalisation.functions]
extended_capitalisation_policy = upper
# ignore_words=

[sqlfluff:rules:convention.quoted_literals]
preferred_quoted_literal_style = single_quotes

# Force the commas to be led
[sqlfluff:layout:type:comma]
line_position = leading
